{
  "name": "llmanui",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild index.ts --bundle --minify --sourcemap --outfile=../dist/index.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
